---
description: Create immersive interactive 3D designs with Three.js
argument-hint: [tasks]
---

**Think hard.** Plan & start working on these tasks following the Orchestration Protocol, Core Responsibilities, Subagents Team and Development Rules:
<tasks>$ARGUMENTS</tasks>

## Methodology

- **Planning:** activate `planning` skill ([.claude/skills/software/planning/SKILL.md](.claude/skills/software/planning/SKILL.md)) — Plan Directory Structure + Plan File Specification (single source of truth).
- **Design skills:** activate `aesthetic` + `frontend-design` + `threejs` + `shader` skills.
- **Auto-activate** other skills from the catalog as relevant.

## Variant: `/design:3d` — Three.js / WebGL specialist

Distinct from siblings (`/design:fast`, `/design:good`, etc.) — this variant produces **immersive 3D experiences** with custom shaders, particle systems, GPU-accelerated rendering.

## Workflow
1. `ui-ux-designer` + `researcher` subagents → 3D design plan following `planning` skill's progressive disclosure structure.
2. `ui-ux-designer` implements plan step by step.
3. Create Three.js scenes with particle effects, custom GLSL shaders, interactive elements.
4. Leverage `ai-multimodal` skills for asset generation + validation.
5. Report to user → ask review/approve.
6. On approval → update `./docs/design-guidelines.md`.

## 3D-Specific Requirements

- Three.js scenes with proper optimization (target 60fps)
- Custom GLSL vertex + fragment shaders
- GPU-accelerated particle systems
- Cinematic camera controls + post-processing
- Responsive across devices
- Interactive elements with smooth animations

## Asset Pipeline (3D-specific)

| Tool | Use |
|---|---|
| `ai-multimodal` skill | Generate textures, skyboxes, env maps, particle sprites, video backgrounds; inpainting/outpainting; visual analysis |
| `imagemagick` skill | Process/optimize textures for WebGL · normal+height maps · sprite sheets · bg removal · masking |

## Implementation Stack
Three.js · GLSL · HTML/CSS/JS · WebGL · post-processing libraries

## Notes
- Test across devices for 60fps performance.
- Document shaders, particle systems, reusable 3D components in `./docs/design-guidelines.md`.
- All plan-structure rules → `planning` skill (single source of truth).
- Concise grammar in reports. List unresolved questions at end.
